##### Form:

* in by default the fast API will receive date with only json format, but if we are going to get a data from form method we should use the function name called **Form()**



##### File:

* 
